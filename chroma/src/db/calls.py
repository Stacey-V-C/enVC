def add_to_chroma(client, results):
    pass

def add_to_chroma_if_not_exists(client, results):
    ids, content, metadata = format_for_chroma(results)

    return

def update_chroma(client, results):
    ids, content, metadata = format_for_chroma(results)

    return

def delete_from_chroma(client, ids):
    pass

def format_for_chroma(results):
    return [], [], []